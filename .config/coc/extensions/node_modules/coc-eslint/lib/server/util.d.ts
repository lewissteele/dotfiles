import { TextDocument, TextEdit } from 'vscode-languageserver';
import { TextDocumentSettings } from './types';
interface Change {
    start: number;
    end: number;
    newText: string;
}
export declare function getAllFixEdits(textDocument: TextDocument, settings: TextDocumentSettings): TextEdit[];
export declare function getChange(oldStr: string, newStr: string): Change;
export declare function resolveModule(name: string, localPath: string, globalPath: string): Promise<string>;
export {};
